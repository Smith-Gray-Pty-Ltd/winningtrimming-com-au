build
dist / media
node_modules
.DS_Store
.env
.env.local
.env.*.local
.env.production
.env.production.local
.next
.vercel

# Payload default media upload directory
public/media/*
!public/media/.gitkeep

# Locally-stored uploads (when S3 is not configured)
/storage

# OrbStack / Docker runtime artefacts
.docker

# Data migration scripts + source data (kept locally, not in repo)
/data-migration

# USM docs build artifacts
.usm-workspace/docs/.vitepress.pid
.usm-workspace/docs/.vitepress.port
.usm-workspace/docs/.vitepress/

# TypeScript build info
tsconfig.tsbuildinfo

